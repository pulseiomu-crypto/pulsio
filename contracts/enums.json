{
  "$comment": "Hand-authored fixed enums shared by iOS, web and NerveCentre (ARCHITECTURE.md §3, kind 2). Codegen to Swift/TS is not built yet: until it is, ios/PulsIO/Contracts/Enums/*.swift mirror these by hand and must be updated in the same commit.",
  "newsCategory": {
    "source": "pulsio_news.category (text, default 'general')",
    "values": [
      "breaking",
      "crime",
      "traffic",
      "weather",
      "community",
      "politics",
      "economy",
      "sports",
      "tourism",
      "general"
    ],
    "fallback": "general"
  },
  "tier": {
    "source": "pulsio_profiles.tier (text, default 'free', CHECK)",
    "values": [
      "free",
      "t1",
      "t2",
      "pro"
    ],
    "fallback": "free"
  },
  "userType": {
    "source": "pulsio_profiles.user_type (text, default 'mauritian', CHECK)",
    "values": [
      "mauritian",
      "tourist",
      "pro"
    ],
    "fallback": "mauritian"
  },
  "language": {
    "source": "pulsio_profiles.language (text, default 'en', CHECK). 'cr' exists in the CHECK but Creole is dropped (SPEC §22); clients never offer it.",
    "values": [
      "en",
      "fr",
      "cr"
    ],
    "fallback": "en"
  }
}
